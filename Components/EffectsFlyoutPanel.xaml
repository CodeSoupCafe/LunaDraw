<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:LunaDraw.Logic.ViewModels"
             x:Class="LunaDraw.Components.EffectsFlyoutPanel"
             x:DataType="viewModels:ToolbarViewModel">
  <Border Background="{AppThemeBinding Light={StaticResource PopupBackgroundLight}, Dark={StaticResource PopupBackgroundDark}}"
          Padding="15"
          StrokeShape="RoundRectangle"
          Stroke="{AppThemeBinding Light={StaticResource PopupBorderLight}, Dark={StaticResource PopupBorderDark}}"
          StrokeThickness="1"
          MinimumWidthRequest="250">
    <StackLayout Spacing="12">
      <Label Text="Effects"
             FontAttributes="Bold"
             FontSize="16"
             HorizontalTextAlignment="Center"/>

      <BoxView HeightRequest="1"
               Color="{AppThemeBinding Light={StaticResource PopupBorderLight}, Dark={StaticResource PopupBorderDark}}"
               Margin="0,5"/>

      <!-- Scatter & Jitter Section -->
      <Label Text="Scatter &amp; Jitter"
             FontAttributes="Bold"
             FontSize="14"/>

      <Label Text="Scatter Radius"
             FontSize="12"/>
      <Slider x:Name="ScatterRadiusSlider"
              Minimum="0"
              Maximum="50"
              Value="{Binding ScatterRadius}"
              ValueChanged="OnScatterRadiusChanged"/>

      <Label Text="Size Jitter"
             FontSize="12"/>
      <Slider x:Name="SizeJitterSlider"
              Minimum="0"
              Maximum="1"
              Value="{Binding SizeJitter}"
              ValueChanged="OnSizeJitterChanged"/>

      <Label Text="Angle Jitter"
             FontSize="12"/>
      <Slider x:Name="AngleJitterSlider"
              Minimum="0"
              Maximum="180"
              Value="{Binding AngleJitter}"
              ValueChanged="OnAngleJitterChanged"/>

      <Label Text="Hue Jitter"
             FontSize="12"/>
      <Slider x:Name="HueJitterSlider"
              Minimum="0"
              Maximum="1"
              Value="{Binding HueJitter}"
              ValueChanged="OnHueJitterChanged"/>

      <BoxView HeightRequest="1"
               Color="{AppThemeBinding Light={StaticResource PopupBorderLight}, Dark={StaticResource PopupBorderDark}}"
               Margin="0,10,0,5"/>

      <!-- Magic Effects Section -->
      <Label Text="Magic Effects"
             FontAttributes="Bold"
             FontSize="14"/>

      <StackLayout Orientation="Horizontal"
                   Spacing="10">
        <Label Text="Rainbow Mode"
               VerticalOptions="Center"
               FontSize="12"/>
        <Switch x:Name="RainbowSwitch"
                Toggled="OnRainbowSwitchToggled"
                HorizontalOptions="EndAndExpand"/>
      </StackLayout>

      <StackLayout Orientation="Horizontal"
                   Spacing="10">
        <Label Text="Glow Effect"
               VerticalOptions="Center"
               FontSize="12"/>
        <Switch x:Name="GlowSwitch"
                Toggled="OnGlowSwitchToggled"
                HorizontalOptions="EndAndExpand"/>
      </StackLayout>

      <Label Text="Glow Radius"
             FontSize="12"/>
      <Slider x:Name="GlowRadiusSlider"
              Minimum="1"
              Maximum="50"
              ValueChanged="OnGlowRadiusChanged"/>

      <Label Text="Glow Color"
             FontSize="12"/>
      <StackLayout Orientation="Horizontal"
                   Spacing="10"
                   HorizontalOptions="Center">
        <!-- Preset Colors -->
        <Border Stroke="{StaticResource Gray500Brush}"
                StrokeShape="RoundRectangle 15"
                StrokeThickness="1"
                HeightRequest="30"
                WidthRequest="30">
          <BoxView Color="#FFFF00"
                   HorizontalOptions="Fill"
                   VerticalOptions="Fill">
            <BoxView.GestureRecognizers>
              <TapGestureRecognizer Tapped="OnGlowColorTapped"
                                    CommandParameter="#FFFF00"/>
            </BoxView.GestureRecognizers>
          </BoxView>
        </Border>
        <Border Stroke="{StaticResource Gray500Brush}"
                StrokeShape="RoundRectangle 15"
                StrokeThickness="1"
                HeightRequest="30"
                WidthRequest="30">
          <BoxView Color="#00FFFF"
                   HorizontalOptions="Fill"
                   VerticalOptions="Fill">
            <BoxView.GestureRecognizers>
              <TapGestureRecognizer Tapped="OnGlowColorTapped"
                                    CommandParameter="#00FFFF"/>
            </BoxView.GestureRecognizers>
          </BoxView>
        </Border>
        <Border Stroke="{StaticResource Gray500Brush}"
                StrokeShape="RoundRectangle 15"
                StrokeThickness="1"
                HeightRequest="30"
                WidthRequest="30">
          <BoxView Color="#FF00FF"
                   HorizontalOptions="Fill"
                   VerticalOptions="Fill">
            <BoxView.GestureRecognizers>
              <TapGestureRecognizer Tapped="OnGlowColorTapped"
                                    CommandParameter="#FF00FF"/>
            </BoxView.GestureRecognizers>
          </BoxView>
        </Border>
        <Border Stroke="{StaticResource Gray500Brush}"
                StrokeShape="RoundRectangle 15"
                StrokeThickness="1"
                HeightRequest="30"
                WidthRequest="30">
          <BoxView Color="#00FF00"
                   HorizontalOptions="Fill"
                   VerticalOptions="Fill">
            <BoxView.GestureRecognizers>
              <TapGestureRecognizer Tapped="OnGlowColorTapped"
                                    CommandParameter="#00FF00"/>
            </BoxView.GestureRecognizers>
          </BoxView>
        </Border>
        <Border Stroke="{StaticResource Gray500Brush}"
                StrokeShape="RoundRectangle 15"
                StrokeThickness="1"
                HeightRequest="30"
                WidthRequest="30">
          <BoxView Color="#FFFFFF"
                   HorizontalOptions="Fill"
                   VerticalOptions="Fill">
            <BoxView.GestureRecognizers>
              <TapGestureRecognizer Tapped="OnGlowColorTapped"
                                    CommandParameter="#FFFFFF"/>
            </BoxView.GestureRecognizers>
          </BoxView>
        </Border>
      </StackLayout>
    </StackLayout>
  </Border>
</ContentView>
